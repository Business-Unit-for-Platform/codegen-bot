name: codegen

on:
  workflow_dispatch:
    inputs:
      target_owner:
        description: "Target GitHub owner/org"
        required: true
        default: "Business-Unit-for-Platform"
      backend_repo:
        description: "Target backend repository"
        required: true
        default: "future-vue-pro"
      frontend_repo:
        description: "Target frontend repository"
        required: true
        default: "future-ui-admin-vue3"
      backend_branch:
        description: "Backend base branch"
        required: true
        default: "master"
      frontend_branch:
        description: "Frontend base branch"
        required: true
        default: "master"
      publish_mode:
        description: "Publish mode"
        required: true
        type: choice
        default: "update_existing_repo_with_pr"
        options:
          - update_existing_repo_with_pr
          - rebuild_from_upstream
      publish_target:
        description: "Which generated targets to publish"
        required: true
        type: choice
        default: "full-stack"
        options:
          - full-stack
          - backend-only
      layout_mode:
        description: "Backend output layout"
        required: true
        type: choice
        default: "future-layout"
        options:
          - yudao-upstream
          - future-layout
      target_private:
        description: "Create rebuilt target repos as private when publish_mode=rebuild_from_upstream"
        required: true
        type: boolean
        default: false
      codegen_engine_repo:
        description: "RuoYi/Yudao codegen engine repository"
        required: true
        default: "Business-Unit-for-Platform/ruoyi-vue-pro"
      codegen_engine_ref:
        description: "RuoYi/Yudao codegen engine ref"
        required: true
        default: "master-jdk17"
      codegen_modules:
        description: "Optional multi-module spec, e.g. ticket:ticket_,map:map_"
        required: false
        default: ""
      codegen_module_name:
        description: "Optional single module name"
        required: false
        default: ""
      codegen_table_prefix:
        description: "Optional single module table prefix"
        required: false
        default: ""
      business_sql_repo:
        description: "Optional repository that contains business SQL, e.g. Business-Unit-for-Debet/requirements"
        required: false
        default: ""
      business_sql_ref:
        description: "Ref for business_sql_repo"
        required: false
        default: "main"
      business_sql_path:
        description: "SQL file or directory path inside business_sql_repo"
        required: false
        default: ""
  push:
    branches: [ main ]
    paths:
      - "sql/**"
      - "tools/**"
      - "templates/**"
      - ".github/workflows/codegen.yml"

permissions:
  contents: read

concurrency:
  group: codegen-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gen:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      TARGET_OWNER: ${{ github.event.inputs.target_owner || 'Business-Unit-for-Platform' }}
      BACKEND_REPO: ${{ github.event.inputs.backend_repo || 'future-vue-pro' }}
      FRONTEND_REPO: ${{ github.event.inputs.frontend_repo || 'future-ui-admin-vue3' }}
      BACKEND_BRANCH: ${{ github.event.inputs.backend_branch || 'master' }}
      FRONTEND_BRANCH: ${{ github.event.inputs.frontend_branch || 'master' }}
      PUBLISH_MODE: ${{ github.event.inputs.publish_mode || 'update_existing_repo_with_pr' }}
      PUBLISH_TARGET: ${{ github.event.inputs.publish_target || 'full-stack' }}
      LAYOUT_MODE: ${{ github.event.inputs.layout_mode || 'future-layout' }}
      TARGET_PRIVATE: ${{ github.event.inputs.target_private || 'false' }}
      CODEGEN_ENGINE_REPO: ${{ github.event.inputs.codegen_engine_repo || 'Business-Unit-for-Platform/ruoyi-vue-pro' }}
      CODEGEN_ENGINE_REF: ${{ github.event.inputs.codegen_engine_ref || 'master-jdk17' }}
      BUSINESS_SQL_REPO: ${{ github.event.inputs.business_sql_repo || '' }}
      BUSINESS_SQL_REF: ${{ github.event.inputs.business_sql_ref || 'main' }}
      BUSINESS_SQL_PATH: ${{ github.event.inputs.business_sql_path || '' }}

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=30

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd="redis-cli ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=30

    steps:
      - name: Checkout this repo
        uses: actions/checkout@v5

      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: Business-Unit-for-Platform

      - name: Checkout ruoyi-vue-pro for codegen engine
        uses: actions/checkout@v5
        with:
          repository: ${{ env.CODEGEN_ENGINE_REPO }}
          ref: ${{ env.CODEGEN_ENGINE_REF }}
          path: ruoyi
          fetch-depth: 1
          token: ${{ steps.app-token.outputs.token }}

      - name: Checkout ruoyi-vue-pro MySQL base SQL
        uses: actions/checkout@v5
        with:
          repository: Business-Unit-for-Platform/ruoyi-vue-pro-mysql
          ref: main
          path: ruoyi-vue-pro-mysql
          fetch-depth: 1
          token: ${{ steps.app-token.outputs.token }}

      - name: Checkout business SQL repo
        if: ${{ env.BUSINESS_SQL_REPO != '' }}
        uses: actions/checkout@v5
        with:
          repository: ${{ env.BUSINESS_SQL_REPO }}
          ref: ${{ env.BUSINESS_SQL_REF }}
          path: business-sql-source
          fetch-depth: 1
          token: ${{ steps.app-token.outputs.token }}

      - name: Prepare business SQL
        if: ${{ env.BUSINESS_SQL_REPO != '' && env.BUSINESS_SQL_PATH != '' }}
        run: |
          set -euo pipefail
          src="${GITHUB_WORKSPACE}/business-sql-source/${BUSINESS_SQL_PATH}"
          dst="${GITHUB_WORKSPACE}/sql/schema"
          mkdir -p "${dst}"
          if [[ -d "${src}" ]]; then
            find "${src}" -type f -name '*.sql' -print0 | while IFS= read -r -d '' file; do
              cp "${file}" "${dst}/$(basename "${file}")"
            done
          elif [[ -f "${src}" ]]; then
            cp "${src}" "${dst}/$(basename "${src}")"
          else
            echo "ERROR: business SQL path not found: ${src}"
            exit 1
          fi
          echo "== prepared business SQL =="
          find "${dst}" -type f -name '*.sql' | sort

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
          cache: maven
          cache-dependency-path: ruoyi/pom.xml

      - name: Install runtime tools
        run: |
          sudo apt-get update
          sudo apt-get install -y mysql-client python3 redis-tools git jq tree gh

      - name: Debug workspace before run
        run: |
          set -x
          echo "github.workspace=${GITHUB_WORKSPACE}"
          echo "runner.os=${RUNNER_OS}"
          echo "pwd=$(pwd)"
          whoami
          uname -a
          java -version
          mvn -version
          python3 --version
          git --version
          jq --version
          mysql --version
          redis-cli --version || true
          echo "== codegen targets =="
          echo "TARGET_OWNER=${TARGET_OWNER}"
          echo "BACKEND_REPO=${BACKEND_REPO}"
          echo "FRONTEND_REPO=${FRONTEND_REPO}"
          echo "BACKEND_BRANCH=${BACKEND_BRANCH}"
          echo "FRONTEND_BRANCH=${FRONTEND_BRANCH}"
          echo "PUBLISH_MODE=${PUBLISH_MODE}"
          echo "PUBLISH_TARGET=${PUBLISH_TARGET}"
          echo "LAYOUT_MODE=${LAYOUT_MODE}"
          echo "CODEGEN_ENGINE_REPO=${CODEGEN_ENGINE_REPO}"
          echo "CODEGEN_ENGINE_REF=${CODEGEN_ENGINE_REF}"
          echo "BASE_SQL_ROOT=${GITHUB_WORKSPACE}/ruoyi-vue-pro-mysql"
          echo "BASE_SQL_FILES=ruoyi-vue-pro.sql"
          echo "BUSINESS_SQL_REPO=${BUSINESS_SQL_REPO}"
          echo "BUSINESS_SQL_REF=${BUSINESS_SQL_REF}"
          echo "BUSINESS_SQL_PATH=${BUSINESS_SQL_PATH}"
          echo "== top level =="
          ls -la
          echo "== workspace tree depth 3 =="
          find "${GITHUB_WORKSPACE}" -maxdepth 3 -mindepth 1 | sort | sed -n '1,300p'
          echo "== key files =="
          ls -la "${GITHUB_WORKSPACE}/tools/codegen" || true
          ls -la "${GITHUB_WORKSPACE}/sql" || true
          ls -la "${GITHUB_WORKSPACE}/ruoyi" || true
          ls -la "${GITHUB_WORKSPACE}/ruoyi-vue-pro-mysql" || true

      - name: Wait MySQL
        run: |
          for i in $(seq 1 60); do
            mysqladmin ping -h 127.0.0.1 -proot --silent && exit 0
            sleep 2
          done
          echo "MySQL not ready"
          exit 1

      - name: Wait Redis
        run: |
          for i in $(seq 1 30); do
            redis-cli -h 127.0.0.1 -p 6379 ping | grep -q PONG && exit 0
            sleep 2
          done
          echo "Redis not ready"
          exit 1

      - name: Debug services
        run: |
          set -x
          mysqladmin ping -h 127.0.0.1 -proot
          redis-cli -h 127.0.0.1 -p 6379 ping
          ss -lntp | grep -E ':3306|:6379' || true

      - name: Run codegen
        id: run-codegen
        env:
          MYSQL_HOST: 127.0.0.1
          MYSQL_PORT: 3306
          MYSQL_USER: root
          MYSQL_PWD: root
          CODEGEN_DB_NAME: ruoyi-vue-pro
          CODEGEN_OUTPUT_DIR: ${{ github.workspace }}/out/generated
          CODEGEN_BASE_PACKAGE: cn.iocoder.yudao
          BASE_SQL_ROOT: ${{ github.workspace }}/ruoyi-vue-pro-mysql
          BASE_SQL_FILES: ruoyi-vue-pro.sql
          CODEGEN_MODULES: ${{ github.event.inputs.codegen_modules || '' }}
          CODEGEN_MODULE_NAME: ${{ github.event.inputs.codegen_module_name || '' }}
          CODEGEN_TABLE_PREFIX: ${{ github.event.inputs.codegen_table_prefix || '' }}
          REDIS_HOST: 127.0.0.1
          REDIS_PORT: 6379
        run: |
          set -x
          echo "CODEGEN_OUTPUT_DIR=${CODEGEN_OUTPUT_DIR}"
          echo "== before run =="
          ls -la "${GITHUB_WORKSPACE}" || true
          ls -la "${GITHUB_WORKSPACE}/out" || true
          bash tools/codegen/run.sh

      - name: Debug after codegen
        if: always()
        run: |
          set +e
          echo "== run-codegen conclusion =="
          echo "${{ steps.run-codegen.conclusion }}"
          echo "== pwd/workspace =="
          pwd
          echo "${GITHUB_WORKSPACE}"
          echo "== out dir =="
          ls -la "${GITHUB_WORKSPACE}/out" || true
          echo "== generated dir =="
          ls -la "${GITHUB_WORKSPACE}/out/generated" || true
          echo "== generated files =="
          find "${GITHUB_WORKSPACE}/out/generated" -type f | sort | sed -n '1,300p' || true
          echo "== directories related to out/generated/ruoyi =="
          find "${GITHUB_WORKSPACE}" -maxdepth 5 -type d | sort | grep -E '/out|generated|ruoyi|surefire-reports' || true
          echo "== surefire reports =="
          find "${GITHUB_WORKSPACE}/ruoyi" -path '*/surefire-reports/*' -type f | sort | sed -n '1,200p' || true
          echo "== tail possible reports =="
          for f in $(find "${GITHUB_WORKSPACE}/ruoyi" -path '*/surefire-reports/*' -type f | sort | head -20); do
            echo "----- $f -----"
            sed -n '1,200p' "$f" || true
          done

      - name: Publish generated code
        if: ${{ steps.run-codegen.conclusion == 'success' }}
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          GITHUB_OWNER: ${{ env.TARGET_OWNER }}
          BACKEND_REPO: ${{ env.BACKEND_REPO }}
          FRONTEND_REPO: ${{ env.FRONTEND_REPO }}
          BACKEND_BRANCH: ${{ env.BACKEND_BRANCH }}
          FRONTEND_BRANCH: ${{ env.FRONTEND_BRANCH }}
          PUBLISH_MODE: ${{ env.PUBLISH_MODE }}
          PUBLISH_TARGET: ${{ env.PUBLISH_TARGET }}
          LAYOUT_MODE: ${{ env.LAYOUT_MODE }}
          TARGET_PRIVATE: ${{ env.TARGET_PRIVATE }}
        run: |
          set -x
          echo "== before publish =="
          ls -la "${GITHUB_WORKSPACE}/out" || true
          ls -la "${GITHUB_WORKSPACE}/out/generated" || true
          find "${GITHUB_WORKSPACE}/out/generated" -type f | sort | sed -n '1,200p' || true
          bash tools/codegen/publish.sh

      - name: Debug after publish
        if: always()
        run: |
          set +e
          echo "== publish workspace snapshot =="
          pwd
          ls -la "${GITHUB_WORKSPACE}" || true
          ls -la "${GITHUB_WORKSPACE}/out" || true
          ls -la "${GITHUB_WORKSPACE}/out/generated" || true
          ls -la "${GITHUB_WORKSPACE}/out/publish" || true
          find "${GITHUB_WORKSPACE}/out" -maxdepth 4 | sort | sed -n '1,300p' || true

      - name: Upload generated code
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: generated-code
          path: |
            ${{ github.workspace }}/out/generated
          if-no-files-found: warn
          retention-days: 7

      - name: Upload debug logs
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: codegen-debug-files
          path: |
            ${{ github.workspace }}/out
            ${{ github.workspace }}/ruoyi/yudao-server/target/surefire-reports
          if-no-files-found: warn
          retention-days: 7
